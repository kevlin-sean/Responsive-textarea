{
  "name": "responsive-textarea-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:lib": "pnpm --filter responsive-textarea build", 
    "build:demo": "pnpm --filter responsive-textarea-demo build", 
    "dev:demo": "pnpm --filter responsive-textarea-demo dev",  
    "lint": "pnpm -r lint",
    "build": "pnpm build:lib && pnpm build:demo" 
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "packageManager": "pnpm@9.0.6" 
}